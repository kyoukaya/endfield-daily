services:
  endfield-daily:
    image: ghcr.io/kyoukaya/endfield-daily:latest
    environment:
      ACCOUNT_TOKEN: ${ACCOUNT_TOKEN}
      SCHEDULE: "0 4 * * *"
      # HEALTH_PORT: "8080"
      # DISCORD_WEBHOOK: ${DISCORD_WEBHOOK}
      # DISCORD_USER: ${DISCORD_USER}
      # NOTIFY_NO_OPS: "true"  # Uncomment to notify for "already checked in" status
    # healthcheck:
    #   test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
    #   interval: 30s
    #   timeout: 5s
    #   retries: 3
    #   start_period: 5s
    restart: unless-stopped
